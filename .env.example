# Supabase (required)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-from-supabase-settings-api

# Optional: for admin-only features (Supabase service role key)
# SUPABASE_SERVICE_ROLE_KEY=

# Base URL for links and redirects (no trailing slash)
# — XAMPP subfolder (e.g. http://localhost/crm/public):
BASE_URL=/crm/public
# — cPanel subdomain (e.g. https://crm.yourdomain.com with document root = public/):
# BASE_URL=/
# — cPanel subfolder (e.g. https://yourdomain.com/crm with document root = crm/public):
# BASE_URL=/crm/public
